name: CI Practise

on:
  push:
    branches:
      - main

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      adservice: ${{ steps.filter.outputs.adservice }}
      cartservice: ${{ steps.filter.outputs.cartservice }}
      checkoutservice: ${{ steps.filter.outputs.checkoutservice }}
      productservice: ${{ steps.filter.outputs.productservice }}
      shippingservice: ${{ steps.filter.outputs.shippingservice }}
      paymentservice: ${{ steps.filter.outputs.paymentservice }}
      currencyservice: ${{ steps.filter.outputs.currencyservice }}
      emailservice: ${{ steps.filter.outputs.emailservice }}
      recommendationservice: ${{ steps.filter.outputs.recommendationservice }}
      shoppingassistant: ${{ steps.filter.outputs.shoppingassistant }}
      loadgenerator: ${{ steps.filter.outputs.loadgenerator }}
      prometheus: ${{ steps.filter.outputs.prometheus }}
      alertmanager: ${{ steps.filter.outputs.alertmanager }}

    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            frontend: ['src/frontend/**']
            adservice: ['src/adservice/**']
            cartservice: ['src/cartservice/**']
            checkoutservice: ['src/checkoutservice/**']
            productservice: ['src/productcatalogservice/**']
            shippingservice: ['src/shippingservice/**']
            paymentservice: ['src/paymentservice/**']
            currencyservice: ['src/currencyservice/**']
            emailservice: ['src/emailservice/**']
            recommendationservice: ['src/recommendationservice/**']
            shoppingassistant: ['src/shoppingassistantservice/**']
            loadgenerator: ['src/loadgenerator/**']
            prometheus: ['monitor/prometheus/**']
            alertmanager: ['monitor/alerts/**']

  set-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
    steps:
      - id: v
        run: echo "version=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

  get-registry:
    runs-on: ubuntu-latest
    outputs:
      registry: ${{ steps.login-ecr.outputs.registry }}
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

  codeInteg:
    needs: [detect-changes, set-version, get-registry]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: frontend
            context: ./src/frontend
            changed: ${{ needs.detect-changes.outputs.frontend }}

          - name: adservice
            context: ./src/adservice
            changed: ${{ needs.detect-changes.outputs.adservice }}

          - name: cartservice
            context: ./src/cartservice/src
            changed: ${{ needs.detect-changes.outputs.cartservice }}

          - name: checkoutservice
            context: ./src/checkoutservice
            changed: ${{ needs.detect-changes.outputs.checkoutservice }}

          - name: productservice
            context: ./src/productcatalogservice
            changed: ${{ needs.detect-changes.outputs.productservice }}

          - name: shippingservice
            context: ./src/shippingservice
            changed: ${{ needs.detect-changes.outputs.shippingservice }}

          - name: paymentservice
            context: ./src/paymentservice
            changed: ${{ needs.detect-changes.outputs.paymentservice }}

          - name: currencyservice
            context: ./src/currencyservice
            changed: ${{ needs.detect-changes.outputs.currencyservice }}

          - name: emailservice
            context: ./src/emailservice
            changed: ${{ needs.detect-changes.outputs.emailservice }}

          - name: recommendationservice
            context: ./src/recommendationservice
            changed: ${{ needs.detect-changes.outputs.recommendationservice }}

          - name: shoppingassistant
            context: ./src/shoppingassistantservice
            changed: ${{ needs.detect-changes.outputs.shoppingassistant }}

          - name: loadgenerator
            context: ./src/loadgenerator
            changed: ${{ needs.detect-changes.outputs.loadgenerator }}

          - name: prometheus
            context: ./monitor/prometheus
            changed: ${{ needs.detect-changes.outputs.prometheus }}

          - name: alertmanager
            context: ./monitor/alerts
            changed: ${{ needs.detect-changes.outputs.alertmanager }}

    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - uses: aws-actions/amazon-ecr-login@v2

      - name: Create ECR repo if missing
        run: |
          aws ecr describe-repositories --repository-names ${{ matrix.name }} || \
          aws ecr create-repository --repository-name ${{ matrix.name }}

      - name: Build image
        if: matrix.changed == 'true'
        run: |
          docker build -t ${{ matrix.name }}:${{ needs.set-version.outputs.version }} ${{ matrix.context }}

      - name: Trivy scan (BLOCKER)
        if: matrix.changed == 'true'
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: ${{ matrix.name }}:${{ needs.set-version.outputs.version }}
          severity: CRITICAL,HIGH
          vuln-type: os,library
          exit-code: 1
          ignore-unfixed: true

      - name: Push image
        if: matrix.changed == 'true'
        run: |
          docker tag ${{ matrix.name }}:${{ needs.set-version.outputs.version }} \
          ${{ needs.get-registry.outputs.registry }}/${{ matrix.name }}:${{ needs.set-version.outputs.version }}

          docker tag ${{ matrix.name }}:${{ needs.set-version.outputs.version }} \
          ${{ needs.get-registry.outputs.registry }}/${{ matrix.name }}:latest

          docker push ${{ needs.get-registry.outputs.registry }}/${{ matrix.name }}:${{ needs.set-version.outputs.version }}
          docker push ${{ needs.get-registry.outputs.registry }}/${{ matrix.name }}:latest

  deploy-infra:
    needs: [codeInteg, get-registry]
    if: needs.codeInteg.result == 'success'
    runs-on: ubuntu-latest
    concurrency:
      group: terraform
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      - run: terraform init -reconfigure
        working-directory: terraform

      - run: |
          terraform plan -out=tfplan \
          -var="frontend_image=${{ needs.get-registry.outputs.registry }}/frontend:latest" \
          -var="ad_image=${{ needs.get-registry.outputs.registry }}/adservice:latest" \
          -var="cart_image=${{ needs.get-registry.outputs.registry }}/cartservice:latest" \
          -var="checkout_image=${{ needs.get-registry.outputs.registry }}/checkoutservice:latest" \
          -var="product_image=${{ needs.get-registry.outputs.registry }}/productservice:latest" \
          -var="shipping_image=${{ needs.get-registry.outputs.registry }}/shippingservice:latest" \
          -var="payment_image=${{ needs.get-registry.outputs.registry }}/paymentservice:latest" \
          -var="currency_img=${{ needs.get-registry.outputs.registry }}/currencyservice:latest" \
          -var="email_Img=${{ needs.get-registry.outputs.registry }}/emailservice:latest" \
          -var="recomandation_image=${{ needs.get-registry.outputs.registry }}/recommendationservice:latest" \
          -var="assitant_image=${{ needs.get-registry.outputs.registry }}/shoppingassistant:latest" \
          -var="load_Img=${{ needs.get-registry.outputs.registry }}/loadgenerator:latest" \
          -var="prometheus=${{ needs.get-registry.outputs.registry }}/prometheus:latest" \
          -var="alertmanager=${{ needs.get-registry.outputs.registry }}/alertmanager:latest"
        working-directory: terraform

      - run: terraform apply -auto-approve tfplan
        working-directory: terraform
